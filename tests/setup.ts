/**
 * Vitest setup file
 *
 * Configures test environment for POP! unit tests.
 * No DOM environment needed for Phase 1 pure function tests.
 */

import { beforeAll, afterAll } from 'vitest';

beforeAll(() => {
  // Any global test setup
});

afterAll(() => {
  // Any global test teardown
});
